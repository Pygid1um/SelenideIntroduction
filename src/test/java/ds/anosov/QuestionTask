Есть ли разница между $("h1 div"); и $("h1").$("div"); - может ли привести к тому что, поиск найдёт разные элементы?
Если может - приведите пример, когда.


$("h1 div") - ищет первый div с предком h1, отрабатывает чуть быстрее, но менее читабелен
$("h1").$("div") - ищет первый элемент h1 и среди его потомков первый div, более читабелен, но в случае если на
странице несколько h1 не факт, что в первом будет div, который нам нужен.